import { DynamicMulterInterceptor } from "./dynamic-multer.interceptor";

describe("DynamicMulterInterceptor", () => {
  it("should be defined", () => {
    expect(new DynamicMulterInterceptor()).toBeDefined();
  });
});
